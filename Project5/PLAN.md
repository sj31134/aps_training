# Project5: APS Tutorial Copy from Project4

- 4 APS Tutorial extensions copied from Project4:
  - BasicExtension
  - PanelExtension
  - ChartsExtension
  - GridExtension
- Set up Vite dev server with proxy to APS API endpoints (aps.http)
- Create root-level `aps.http` like in Project4
- Create front-end assets: `index.html`, `app.mjs`
- Add `vite.config.js` and `package.json` for dev workflow
- Move root PLAN.md as needed to root; keep a local PLAN.md in Project5
- Ensure 4 toolbar buttons appear and function

